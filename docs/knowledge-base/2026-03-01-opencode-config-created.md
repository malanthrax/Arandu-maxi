# 2026-03-01 — OpenCode Project Config Created

- Added `opencode.json` at repo root to explicitly bind OpenCode session context to
  `H:\Ardanu Fix\Arandu-maxi`.
- Config content now references project guidance files:
  - `AGENTS.md`
  - `WORKING_DIRECTORY_WARNING.md`
- No build/test changes were required for this update.
- Next follow-up: load OpenCode from the canonical H: path and confirm it now picks up
  this config automatically.
